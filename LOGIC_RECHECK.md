# 📋 รายงานการตรวจสอบ Logic (Logic Recheck)

**วันที่ตรวจสอบ:** _______________  
**ระบบ:** NITESA - ระบบนิเทศ ติดตาม และประเมินผลการศึกษา

---

## 📋 สรุปผลการตรวจสอบ

| หัวข้อ | สถานะ | หมายเหตุ |
|--------|--------|----------|
| Workflow สถานะ (DRAFT → SUBMITTED → APPROVED → PUBLISHED) | ✅ ถูกต้อง | Model + UI สอดคล้องกัน |
| สิทธิ์ Submit (ผู้สร้าง/Admin) | ✅ ถูกต้อง | canSubmit() ตรวจทั้ง status และ role |
| สิทธิ์ Approve/Reject (Admin/Executive) | ✅ ถูกต้อง | canApprove/canReject ใช้ role |
| สิทธิ์ Publish (Admin เท่านั้น) | ✅ ถูกต้อง | canPublish() จำกัด Admin |
| สิทธิ์แก้ไข (DRAFT/NEEDS_IMPROVEMENT + เจ้าของหรือ Admin) | ✅ แก้ไขแล้ว | ตรวจใน SupervisionForm::mount() |
| Sync ตัวชี้วัด (รวมกรณี array ว่าง) | ✅ แก้ไขแล้ว | syncIndicators จัดการ [] ชัดเจน |
| Validation ตัวชี้วัดอย่างน้อย 1 รายการ | ✅ แก้ไขแล้ว | indicators required + min:1 |
| Acknowledge เฉพาะ PUBLISHED และยังไม่รับทราบ | ✅ ถูกต้อง | AcknowledgeForm::mount() ตรวจแล้ว |
| การเข้าถึง Supervision/Attachment | ✅ ถูกต้อง | ใช้ canAccessSupervision |

---

## 1. Workflow การนิเทศ

```
DRAFT ──submit──► SUBMITTED ──approve──► APPROVED ──publish──► PUBLISHED
                        │
                        └──reject──► NEEDS_IMPROVEMENT ──(แก้ไขแล้ว)──► submit กลับไป
```

- **canSubmit():** เฉพาะ DRAFT หรือ NEEDS_IMPROVEMENT  
- **canApprove() / canReject():** เฉพาะ SUBMITTED  
- **canPublish():** เฉพาะ APPROVED  
- Model `Supervision` ใช้ enum และ method ตามนี้ครบ

---

## 2. สิทธิ์ตามบทบาท (UI + Logic)

| การกระทำ | Admin | Executive | Supervisor | School |
|----------|-------|-----------|------------|--------|
| สร้างการนิเทศ | ✅ | ❌ | ✅ | ❌ |
| แก้ไข (DRAFT/NEEDS_IMPROVEMENT) | ✅ ทุกรายการ | ❌ | ✅ เฉพาะของตัวเอง | ❌ |
| ส่งเพื่ออนุมัติ | ✅ | ❌ | ✅ เฉพาะของตัวเอง | ❌ |
| อนุมัติ / ส่งกลับ | ✅ | ✅ | ❌ | ❌ |
| เผยแพร่ | ✅ | ❌ | ❌ | ❌ |
| รับทราบ (Acknowledge) | ❌ | ❌ | ❌ | ✅ (เมื่อ PUBLISHED) |
| ดูรายการ | ตาม list filter | ตาม list filter | เฉพาะโรงเรียนที่รับผิดชอบ | เฉพาะ PUBLISHED |

- **SupervisionShow:** ใช้ `canSubmit()`, `canApprove()`, `canReject()`, `canPublish()`, `canEdit()` ตามบทบาทและเจ้าของ  
- **SupervisionForm (แก้ไข):** ใน `mount()` ตรวจว่าเป็น Admin หรือ Supervisor เจ้าของ และสถานะเป็น DRAFT หรือ NEEDS_IMPROVEMENT แล้วจึงให้โหลดฟอร์ม

---

## 3. การแก้ไขที่ทำในรอบตรวจ Logic

### 3.1 SupervisionService::syncIndicators

- **ปัญหา:** เมื่อส่ง `$indicators = []` (array ว่าง) การใช้ `whereNotIn('id', [])` ใน Laravel อาจไม่ลบตัวชี้วัดที่เหลือ  
- **แก้ไข:** ถ้า `$indicators` ว่าง ให้ `$supervision->indicators()->delete()` เลย แล้ว return  
- **ผล:** การ sync “ลบตัวชี้วัดทั้งหมด” เมื่อได้ array ว่างทำงานชัดเจน (และจะไม่เกิดจากฟอร์มเพราะมี validation ด้านล่าง)

### 3.2 SupervisionForm – Validation ตัวชี้วัด

- **ปัญหา:** กฎเดิม `'indicators' => ['array']` ไม่บังคับอย่างน้อย 1 รายการ  
- **แก้ไข:** ใช้ `'indicators' => ['required', 'array', 'min:1']` และเพิ่มข้อความ `indicators.required` / `indicators.min`  
- **ผล:** ฟอร์มไม่สามารถส่งตัวชี้วัดว่างได้ และสอดคล้องกับ StoreSupervisionRequest

### 3.3 SupervisionForm::mount (แก้ไข)

- **ปัญหา:** Route แก้ไขใช้ middleware เฉพาะ role ADMIN,SUPERVISOR แต่ไม่ตรวจว่า Supervisor เป็นเจ้าของการนิเทศหรือไม่ และไม่ตรวจสถานะ  
- **แก้ไข:** ใน `mount()` เมื่อเป็นโหมดแก้ไข ตรวจว่า  
  - เป็น Admin หรือ (Supervisor และ `$supervision->userId === $user->id`)  
  - และสถานะเป็น DRAFT หรือ NEEDS_IMPROVEMENT  
  ถ้าไม่ผ่าน → `abort(403)`  
- **ผล:** แก้ไขได้เฉพาะรายการที่ “แก้ได้” ตามสถานะ และเฉพาะเจ้าของหรือ Admin

---

## 4. Logic อื่นที่ตรวจแล้ว (ไม่ต้องแก้)

- **AcknowledgeForm:** ตรวจ status === PUBLISHED และยังไม่มี acknowledgement แล้วค่อยให้รับทราบ  
- **SupervisionList:** กรองตาม role (โรงเรียนที่รับผิดชอบ / PUBLISHED สำหรับ School)  
- **AttachmentController:** ใช้ `authorizeAttachment()` → `canAccessSupervision()`  
- **SupervisionShow::mount():** ใช้ `canAccessSupervision()` ก่อนโหลดรายละเอียด  
- **IndicatorLevel:** score 1–4, label/color ตรงกับ enum  
- **Supervision::getAverageIndicatorScoreAttribute:** คำนวณจาก `indicators` และ `level->score()` ถูกต้อง  

---

## 5. ไฟล์ที่แก้ไขในรอบ Logic Recheck

| ไฟล์ | การเปลี่ยนแปลง |
|------|------------------|
| `app/Services/SupervisionService.php` | syncIndicators: จัดการกรณี `$indicators === []` โดยลบตัวชี้วัดทั้งหมด |
| `app/Livewire/Supervision/SupervisionForm.php` | 1) rules: indicators required + min:1 และ messages 2) mount: ตรวจสิทธิ์แก้ไขและสถานะก่อนโหลดฟอร์มแก้ไข |

---

## 6. สรุป

- Workflow สถานะและสิทธิ์ Submit/Approve/Reject/Publish/Edit ตรงกับบทบาทและเจ้าของ  
- การ sync ตัวชี้วัดและ validation ตัวชี้วัดสอดคล้องกัน และรองรับกรณี array ว่างอย่างชัดเจน  
- การแก้ไขการนิเทศถูกจำกัดทั้งที่ route และที่ component (mount) ให้สอดคล้องกับนโยบายสิทธิ์
