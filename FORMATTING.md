# 📐 Code Formatting

คู่มือการจัดรูปแบบโค้ด (Formatting) สำหรับโปรเจค NITESA

---

## ⚡ Quick Commands

```bash
# จัดรูปแบบ PHP ทั้งโปรเจค (แนะนำก่อน commit)
composer format

# ตรวจสอบเท่านั้น (ไม่แก้ไฟล์)
composer format:check

# หรือใช้ชื่อเดิม
composer lint:fix
composer lint:php:check
```

---

## 📋 เครื่องมือที่ใช้

| เครื่องมือ | ใช้สำหรับ | Config |
|------------|-----------|--------|
| **PHP CS Fixer** | จัดรูปแบบ PHP (PSR-12, PHP 8.1) | `.php-cs-fixer.dist.php` |
| **EditorConfig** | ความสอดคล้อง indent, newline, charset | `.editorconfig` |

---

## 📐 กฎการจัดรูปแบบ (สรุป)

- **PSR-12** – Coding style มาตรฐาน PHP
- **PHP 8.1** – โค้ดเหมาะกับ PHP 8.1+
- **Indent** – 4 spaces
- **Array** – short syntax `[]`
- **Imports** – เรียงตาม alpha, ลบที่ไม่ได้ใช้
- **Trailing comma** – มีใน multiline arrays/arguments
- **Whitespace** – `!` มี space หลัง (`! $x`), บรรทัดว่างก่อน `return`/`throw`/`try`
- **New instantiation** – `(new Foo())` มีวงเล็บ

---

## 🔧 รันแบบละเอียด

```bash
# จัดรูปแบบทั้งโปรเจค
vendor\bin\php-cs-fixer fix

# จัดเฉพาะโฟลเดอร์
vendor\bin\php-cs-fixer fix app/
vendor\bin\php-cs-fixer fix app/Http/

# ตรวจสอบเท่านั้น (แสดง diff)
vendor\bin\php-cs-fixer fix --dry-run --diff

# จัดรูปแบบเฉพาะไฟล์
vendor\bin\php-cs-fixer fix app/Services/SupervisionService.php
```

---

## 📁 EditorConfig

โปรเจคใช้ `.editorconfig` เพื่อให้ editor จัดรูปแบบให้สอดคล้องกัน:

- **Charset:** UTF-8
- **End of line:** LF
- **Indent:** 4 spaces
- **Trim trailing whitespace:** เปิด
- **Insert final newline:** เปิด

รองรับโดย VS Code, Cursor, PhpStorm, Sublime ฯลฯ (อาจต้องติดตั้ง extension)

---

## ✅ แนะนำก่อน Commit

1. รัน `composer format` ให้ผ่าน
2. หรือเปิด Format on Save ใน editor แล้ว save ไฟล์ที่แก้
3. Commit เมื่อไม่มี diff จาก formatter

---

## 📁 ไฟล์ที่เกี่ยวข้อง

- `.php-cs-fixer.dist.php` – config PHP CS Fixer
- `.editorconfig` – config สำหรับ editor
- `.php-cs-fixer.cache` – cache (ไม่ commit)

---

**อัปเดตล่าสุด:** ตามการตั้งค่าในโปรเจคปัจจุบัน
